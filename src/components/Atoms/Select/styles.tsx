import styled from 'styled-components';
import { SITE_COLOR } from '../../../styles/valiables';

export const StyledSelectWrap = styled.label``;
export const StyledSelect = styled.select`
  border: 1px solid ${SITE_COLOR.BORDER_COLOR};
  display: block;
  font-size: 1.7rem;
  padding: .3em;
  width: 100%;
`;
